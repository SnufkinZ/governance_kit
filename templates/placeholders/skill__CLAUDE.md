# docs/skill — authoring skills (placeholder)

> Installs as `docs/skill/CLAUDE.md`. Keep it a `CLAUDE.md`, not a README, and
> keep it orientation-only: the map below plus a one-line gloss per entry.

Reusable "how to write X" guides. Read the relevant one **before** authoring
that kind of document — they carry the format contracts the Tier 1 tests
enforce.

```
docs/skill/
|-- CLAUDE.md                  # this map
|-- design_template.md         # how to write a design_*.md (the L2 contract)
|-- in_process_plan_format.md  # the plan-doc head + Track format the board expects
`-- document_maintenance.md    # the draft-first doc-sync workflow (keep docs current while coding)
```
