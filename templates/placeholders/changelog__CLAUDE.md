# Changelog (placeholder)

> Installs as `docs/changelog/CLAUDE.md`. Keep it a `CLAUDE.md` — this
> directory carries a local map, not a README (see `docs/CLAUDE.md`
> conventions).

**Change history lives here.** One file per design doc, mirroring its versions.

- Records *what changed* (ADRs record *what was considered and why* — see `../decisions/`).
- Append-only in spirit: supersede old entries, do not rewrite them.
- A `design_*.md` that declares `**Version:** X` must have a matching entry here
  for `X` (enforced by Tier 2 — `design_doc_sync.md` §3).

Empty for now. This placeholder exists so the AI can see the slot from day one
(BOOTSTRAP.md axiom A2).
