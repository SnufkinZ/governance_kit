# Audit reports (placeholder)

> Installs as `docs/audit/CLAUDE.md`. Keep it a `CLAUDE.md`, not a README.

**Semantic-drift audit reports live here** (Tier 4 — `design_doc_sync.md` §5).

Each report: `YYYY-MM-DD_<scope>.md`, listing — for each drifted doc — its
location, the drift, the evidence (code vs. doc), and a suggested reconciliation.
The audit **reports**; a human turns findings into doc edits.

Empty for now. Tier 4 runs at milestones, and is only put on a cadence at Phase
1→2 (BOOTSTRAP.md R0.3). This placeholder exists so the slot is visible from day
one (axiom A2).
